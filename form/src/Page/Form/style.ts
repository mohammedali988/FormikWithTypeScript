import { makeStyles } from "@mui/styles";

export const useStyle = makeStyles({
  container: {
    width: "50% !important",
    margin: " 80px auto",
    display: "flex",
    minHeight: "100vh",
    flexDirection: "column",
  },
  paper: {
    padding: "50px",
  },
});
